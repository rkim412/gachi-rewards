{
  "buildCommand": "git config --global user.name 'Vercel' && git config --global user.email 'vercel@vercel.com' && npm run build && npm run setup",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/build/server/index.js"
    }
  ]
}
